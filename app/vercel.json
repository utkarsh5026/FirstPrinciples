{
  "version": 2,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/content/(.*)", "dest": "/content/$1" },
    { "src": "/documents/(.*)", "dest": "/index.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}